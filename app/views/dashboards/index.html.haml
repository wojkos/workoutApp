.jumbotron
  %h1 Workout Lounge!

  %p Show off your workout
.row
  .col-md-10
    %h2 List of Members
    =will_paginate @athletes, renderer: BootstrapPagination::Rails, class: 'pull-left paginate'

.row
  -@athletes.each do |athlete|
    .col-md-10.col-md-offset-2.well.member
      =athlete.full_name
      - if current_user
        = link_to('Follow', friendships_path(friend_id: athlete.id), method: :post, class: 'btn btn-success pull-right') unless current_user.follow_or_same?(athlete)